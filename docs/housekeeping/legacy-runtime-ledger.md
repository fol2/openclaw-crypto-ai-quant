# Legacy Runtime Ledger

## Status

Phase 3 is complete. The repository no longer carries the legacy alternate
runtime tree.

## Removed During The Zero-Language-Cleanup Cutover

- legacy runtime source directories
- legacy operator and parity tool tree
- legacy test suite tied to the removed runtime
- legacy packaging metadata and lockfiles
- legacy monitor and service examples tied to the removed runtime

## Remaining Active Surfaces

- Rust runtime
- Rust backtester
- Rust WS sidecar
- Rust hub
- shell wrappers and Cargo-based release tooling
