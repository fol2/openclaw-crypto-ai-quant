# AI Quant Trading — local-only runtime/state

# Python
__pycache__/
*.pyc

# Virtualenv
venv/
.venv/

# SQLite + locks
*.db
*.db.*
*.db-shm
*.db-wal
*.db-journal
*.lock
# Lockfiles should be tracked (dependency reproducibility)
!uv.lock
!Cargo.lock
!**/Cargo.lock
*.bak*

# Logs
daemon_log*.txt
live_daemon_log*.txt
monitor_log*.txt

# Secrets / env
secrets.json
.env
.env.*
*.key
*.pem
# Keep templates
!.env.example

# Generated artifacts
*.png

# Generated analysis outputs (reproducible)
backtest_results.json
analyst_state.json

# Test artifacts
.coverage
.pytest_cache/

# Rust build artifacts
backtester/target/
ws_sidecar/target/

# Candle databases (large, fetched at runtime)
candles_dbs/

# Archive (old zips, DB backups, legacy scripts)
archive/

# Exports (trade CSV data)
exports/

# Sweep — track scripts, ignore configs + results
sweep_full/*.yaml
sweep_full/*.csv
sweep_full/*.json
sweep_full/*.jsonl
sweep_full/results/
sweep_full/results_*/

# Linter cache
.ruff_cache/

# Runtime state
strategy_changelog.json

# Timestamped sweep configs (generated, not canonical)
strategy_tpe_*.yaml

# Developer-local files (ad-hoc scripts, experiments, temp)
local/

# Reports (generated analysis markdown)
reports/
